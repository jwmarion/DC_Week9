<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
    <link href="/style.css" rel="stylesheet">
  </head>
  <body>
    {{#if session.loggedInUser}}
      <p>Welcome, {{session.loggedInUser}}!</p>
    {{/if}}
    {{{ body }}}
  </body>
</html>
